# Copy to .env.local and fill in your values.
# The app works without a Gemini key — falls back to keyword extraction.
GEMINI_API_KEY=

# SQLite database (local file). No setup needed — Prisma creates it.
DATABASE_URL="file:./dev.db"
